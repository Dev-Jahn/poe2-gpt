# Compatibility overlay. compose.engine.yaml enables managed build projections.
services:
  poe2-companion:
    environment:
      POE2_BUILD_PROJECTION_DIR: /build-projections
    volumes:
      - build-projections:/build-projections:ro
volumes:
  build-projections:
